.modal#flash-modal
	.modal-header{:class => @flash_key}
		%button.close{:data => {:dismiss => "modal"}} ×
		%h3= app_title
	.modal-body
		%p= @flash_value
	.modal-footer
		%button.btn{:data => {:dismiss => "modal"}} Close